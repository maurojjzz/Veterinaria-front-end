import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#252F48",
    },
    secondary: {
      main: "#A9C3B8",
    },
  },
});

export default theme;
